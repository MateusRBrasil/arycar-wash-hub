version: "3.8"

services:
  arycar-frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:4000/api}
    ports:
      - "3000:80"
    restart: unless-stopped
